# Kök .env — yalnızca docker compose tarafından okunur.
# Backend'in kendi ayarları backend/.env dosyasındadır (bkz. backend/.env.example);
# oradaki DATABASE_URL'in şifresi buradakiyle aynı olmalı.
POSTGRES_PASSWORD=SIFRE
